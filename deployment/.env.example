# Puter Docker 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# 时区设置
TZ=Asia/Shanghai

# 用户 ID 和组 ID（默认 1000:1000）
PUID=1000
PGID=1000

# 如果需要自定义配置文件路径，取消注释下面的行
# CONFIG_PATH=/etc/puter

# 端口映射（默认 4100:4100）
# PORT=4100
